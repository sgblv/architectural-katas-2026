# Diagrams

Add source files (`.mmd`, `.drawio`) alongside exported `.png`/`.svg`. Keep them simple and include a key.

| Diagram | What it shows |
|---------|---------------|
| _example-context.png_ | _System context_ |
